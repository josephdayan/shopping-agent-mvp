node_modules
.next
.vercel
.env
.env.local
prisma/dev.db
prisma/dev.db-journal
dist
coverage

# Browser sessions contain retailer authentication cookies and must never be committed.
playwright/.auth/
.browserbase/

# TypeScript incremental build info
tsconfig.tsbuildinfo

# macOS / editor / R junk
.DS_Store
**/.DS_Store
.Rhistory
.claude/*
!.claude/agents/
LIA_WHATSAPP.md
# Vault do Obsidian (config de editor, não é doc do projeto)
docs/obsidian/.obsidian/
/.swc
.env*
